// Legacy file - replaced by src/config/supabase.js
// Kept to avoid breaking any indirect imports
export default async function connectDB() {
  // Connection is now handled directly via the Supabase client in server.js
}
